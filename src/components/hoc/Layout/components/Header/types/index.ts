export type HeaderProps = {
    handleFullscreen: () => void
    fullscreen: boolean
}

export type useHeaderProps = {
    handleFullscreen: () => void
    fullscreen: boolean
}

export type Header = {
    id: string
}