import { SxProps, Theme } from '@mui/material'

const sx: Record<string, SxProps<Theme>> = {
    root: {
        color: 'text.secondary',
        userSelect: 'none',
        cursor: 'pointer'
    },
}

export default sx